# Generate TWO different random keys (at least 32 characters); never commit .env.
AI_ADMIN_KEY=
AI_REPLY_KEY=
# Bind to a private/VPN address if CRM is on another server.
AI_BIND_HOST=127.0.0.1
AI_PORT=8088
# Optional token for a protected Ollama gateway. Never returned by the API.
OLLAMA_API_KEY=
